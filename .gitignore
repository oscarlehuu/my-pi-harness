# Machine-local runtime files — ~/.pi/agent is live; this repo only stores harness source.
agent/auth.json
agent/settings.json
agent/sessions/
agent/bin/
agent/*-debug.log

# OS / editor noise
.DS_Store
.antigravitycli/
.claude/

# Ledger noise inside any project we run (only .pi/plans/ ledger is committed in target repos)
*.log
*.tmp
