# Machine-local runtime (auth.json, settings.json, sessions/) lives only in ~/.pi/agent,
# never in this repo. Nothing to ignore here for it.

# OS / editor noise
.DS_Store
.antigravitycli/
.claude/

# Ledger noise inside any project we run (only .pi/plans/ ledger is committed in target repos)
*.log
*.tmp

# Scratch / throwaway working notes (not part of the published workspace)
plans/reports/
extensions/grok/grok-build-vs-harness-video-gap-prompt.md
